using System;
using System.Linq;
using System.Collections.Generic;

class Program
{
    static int width, height, time, last;
    static int[] dr = { 1, 0, -1, 0 };
    static int[] dc = { 0, 1, 0, -1 };
    static Queue<int> exposed = new Queue<int>();

}